Для вывода данных используется `System.out.println("text");`
Чтобы вывести что-то не перенося строку можно использовать `System.out.print("text");`
Для печати с форматированием используется `System.out.printf("pi is %.5f", Math.PI);`