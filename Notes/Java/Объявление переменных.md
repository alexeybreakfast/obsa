Переменные объявляются в формате
`type name;`
Где `type` - тип, а `name` - название переменной

Чтобы объявить сразу несколько переменных используется
`type name1, name2, name3;`

Чтобы объявить и задать значение переменной используется
`type name = val`
где `val` - значение