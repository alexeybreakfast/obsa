**Под списками имеются в виду массивы и вектора**
## Сортировка
``` cpp
#include<ranges>
int list[3] = {3, 1, 2}; 

ranges::sort(list);
```

## Максимальное
``` cpp
#include<ranges>
#include<algorithm>
int list[3] = {1, 284, 3};

int max_val = ranges::max(list);
```

## Минимальное
```cpp
#include<ranges>
#include<algorithm>
int list[3] = {1, -24398, 3};

int min_val = ranges::min(nums);
```

## Сумма
``` cpp
#include<numeric>
#include<ranges>
int list[3] = {2, 4, 3};

int sum = ranges::accumulate(list)
```