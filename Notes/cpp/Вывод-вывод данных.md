Вывод/вывод данных делается через системную библиотеку `iostream`
``` c++
#include<iostream>

int num1, num2;

std::cout << "Hello world";
std::cin >> num1 >> num2;
```
тут мы выводим на экран `Hello world` и вводим с клавиатуры `num1` и `num2` (не в одну строчку, а построчно)