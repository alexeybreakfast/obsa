Что-то типо `updater` в  `go` тут делается через
``` rust
let listener = TcpListener::bind("0.0.0.0:port")
```

Запускаться всё должно в асинхроне, короче чтобы не ждали семеро одного надо дописывать где-то в конце почти каждой функции волшебную фразу `.await.unwrap()`
