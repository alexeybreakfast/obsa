Cargo это быстрый менеджер проектов на rust, чтобы создать новый проект достаточно прописать `cargo new <name>`, тогда он создать директорию с именем name и всю структуру проекта в ней, в `cargo.toml` прописывается вся информация о проекте