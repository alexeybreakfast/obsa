Замыкания в `rust` это способ задать функцию как переменную, например
``` rust
let func = |a: i32, b: i32| {
	let x = a+b;
	x
}
```
При вызове `func` будет вызываться функция описанная выше, где `a`,`b` — параметры, и будет возвращаться их сумма